# M9 发布候选验收清单

> 本文前 3 节保留 2026-08-13 的历史验收记录；最新暂停快照说明见第 4 节。

- 验收日期：2026-08-13
- 当前版本：`0.1.0`
- 当前结论：AC-01 至 AC-15 全部通过，本地工作树满足 v0.1 发布条件
- 发布动作：未 commit、未 push、未创建 PR、未发布镜像

## 1. 验收场景

| 场景 | 状态 | 证据与结论 |
|---|---|---|
| AC-01 首次设置与会话失效 | 通过 | Auth/API/UI 测试覆盖一次性 Setup Token、Argon2id、滑动会话、密码重置和旧会话撤销；浏览器完成首次设置与登录。 |
| AC-02 多榜单合并与预演 | 通过 | Pipeline、Run Service 和离线浏览器预演保留排名证据，`items_submitted=0`。 |
| AC-03 候选多源聚合 | 通过 | 同一 BTIH 的多来源 Evidence 被聚合；浏览器 Run 详情同时显示 `javdb_detail` 与 `knaben_api`。 |
| AC-04 三态规则与解释 | 通过 | 规则真值表、未知体积/做种数与多原因拒绝由参数化测试覆盖。 |
| AC-05 手工番号与直接 magnet | 通过 | 服务/API/浏览器覆盖番号持久 Run、候选确认、BTIH 规范化、直接 magnet 预览和非法输入。 |
| AC-06 全局去重 | 通过 | 内容与 BTIH 跨 Profile 幂等、原任务关联、代次和强制确认由数据库/服务/API/浏览器覆盖。 |
| AC-07 候选回退 | 通过 | 明确同步拒绝、结果不明和 CD2 接受后失败的不同策略由 Run/Download 测试覆盖。 |
| AC-08 来源部分失败 | 通过 | `success`、`partial`、`failed`、空结果与来源健康隔离均有 Fixture 测试。 |
| AC-09 CD2 合法测试下载与完成 | 通过 | 受限 Token 补充最小 `CreateFolder` 权限后，连接测试成功创建独立测试目录；WebTorrent 合法开放影片由当前实现提交到“Profile / 完整 BTIH”，自动经历 `accepted → downloading → completed`。完成依据为两次稳定文件快照，115 目标目录实见 6 个文件、约 210.6 MB，无人工完成。 |
| AC-10 需要关注与人工完成 | 通过 | 阈值、去重锁、持续轮询、二次确认和人工来源事件由状态机/API/浏览器覆盖。 |
| AC-11 调度与配置快照 | 通过 | 重叠触发、受控并发、停机不补跑、不可变快照和优雅调度器关闭均有测试。 |
| AC-12 秘密与日志 | 通过 | 设置/API/OpenAPI/日志脱敏覆盖 Cookie、Password、Token 与完整 Authorization；扫描未发现真实秘密。 |
| AC-13 迁移、备份与恢复 | 通过 | 0001–0004 迁移、失败拒绝启动、原子替换、恢复和最多三份备份由自动化测试覆盖。 |
| AC-14 CSV 与归档 | 通过 | CSV BOM、字段、公式转义以及 Profile 归档/恢复和历史保留均有测试。 |
| AC-15 Docker 构建 | 通过 | 在临时隔离的 Colima/Buildx 环境实际生成同时包含 Linux `amd64`、`arm64` 的 OCI 索引，并分别启动两种架构；AMD64 容器内确认为 `x86_64`。两者均以 UID/GID `10001:10001` 运行，启用只读根文件系统、`cap_drop: ALL` 和 `no-new-privileges`，`/data` 重启持久化、健康与就绪检查正常。仓库 `compose.yaml` 另行完成构建、启动、健康等待、重启持久化和 SIGTERM 优雅关闭，退出码为 0。 |

## 2. 本地质量门槛

正式版最终工作树验证结果：

- `.venv/bin/python -m pytest -q`：174 项测试、49 项参数化子测试全部通过；仅有本机 Python 3.14 下 FastAPI TestClient 对旧 httpx 适配层的弃用警告。
- `compileall` 和 `pip check`：通过，无损坏依赖。
- `pip-audit`：`No known vulnerabilities found`。
- Ruff `F/E9/EXE`：项目源码、测试和脚本通过；自动生成的 protobuf 客户端按生成物排除，不手工改写。
- wheel：`tuntu-0.1.0-py3-none-any.whl` 可构建、隔离安装并导入，包含 Jinja、静态资源、Alembic 0001–0004 和官方 CD2 proto；SHA-256 为 `23348ca2aece08f292fb2098021e41afc9b86a0978e284a22d2a13e3c4fff801`。
- `git diff --check`：通过。
- 产品源码扫描：不包含开发者私网地址、`/115open`、真实凭据或生产磁力。
- 375px 和桌面浏览器关键路径无横向溢出，控制台无错误；所有验收服务均已优雅停止。

## 3. 发布状态

正式版门槛已经解除，版本号已提升为 `0.1.0`。本轮只形成可发布的本地工作树和构建证据；Git commit、push、PR 与远端镜像发布仍需用户明确指令。

## 4. 暂停快照（2026-09-02）

- 按维护者要求暂停开发，保留当前 `0.1.0` 源码、测试、配置示例和文档。
- 暂停前重新运行全量测试：198 项测试、53 项参数化子测试通过；保留一项 Starlette/httpx 弃用警告。
- GitHub 快照排除数据库、日志、备份、虚拟环境及真实账号凭据和 Cookie；本地运行数据不属于代码备份。
- 已知限制：网页源可能需要有效会员会话，仍会受到限流或页面变化影响；部分非标准番号的精确搜索匹配尚待增强。
- 本次不发布容器镜像，也不改变已有云盘文件或离线任务。
